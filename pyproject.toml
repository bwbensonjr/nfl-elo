[project]
name = "nfl-elo"
version = "0.1.0"
description = "NFL Elo rating calculator"
requires-python = ">=3.10"
dependencies = [
    "pandas>=2.2.2",
    "tabulate>=0.9.0",
]

[dependency-groups]
dev = []
